# TODO: bug fixes
- [] sheild animation appears which players run out of matter
    - behavior should be sheild animation appears on hit and is color of player
    - sheild disapears when out of matter and exposed
    - screen of player exposed flashes a bit as a warning

- [] melee on player that is not exposed only pushes them up as if they were jumping
    - should push them up and in the melee direction knocking them back

- [] melee should have a recharge time independent of bullet firing and removes a voxelBrushSpan area instead of 1 voxel
- [] building should build a voxelBrushSpan voxel instead of 1
 - should have a recharge time independent of bullet firing and melee

